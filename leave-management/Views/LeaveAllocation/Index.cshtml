@model leave_management.Models.CreateLeaveAllocationVM

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


<table id="tblData" class="table">
    <thead>
        <tr>
            
            <th>
                Leave Types
            </th>
            
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LeaveTypes) {
        <tr>
          
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
         
            <td>
                
                 @*@Html.ActionLink("Allocate To Empoyees", "SetLeave", new { id = item.Id })//SetLeave is fun we going to call it from the controller L24,M15:23, and we passs id*@
            <a class="btn btn-outline-success" asp-action="SetLeave" asp-route-id="@item.Id"><i class="fa fa-wrench" aria-hidden="true"></i>  Allocate To Empoyees </a>
                

            </td>
        </tr>
}
    </tbody>
</table>
